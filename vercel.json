{
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "installCommand": "npm install && DATABASE_URL=file:/tmp/dev.db ./node_modules/.bin/prisma generate --schema=backend/prisma/schema.prisma && DATABASE_URL=file:/tmp/dev.db ./node_modules/.bin/prisma db push --schema=backend/prisma/schema.prisma --accept-data-loss",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "env": {
    "DATABASE_URL": "file:/tmp/dev.db",
    "JWT_SECRET": "rfid-attendance-jwt-secret-key-2024",
    "JWT_EXPIRES_IN": "7d"
  }
}
